以下是我在某個 AI 工具的 prompt, 陸續會生出影片，我希望 prompt 生成，跟給我兒子看的影片（未來不排除有圖片），可以用這個 repo. 專案來處理，重點是：

1. 6-8 歲小朋友極度友善的影片或圖片觀看體驗（有趣，不知不覺學習）
2. 我給回饋就可以不斷精進，相對自動的 prompt 生成器，我直接輸入到訂閱的 AI ，每天有 3 個影片額度，多個圖片額度


[參考 prompt]
# Role
你是一個專門生成「8 秒幼兒識字動畫影片 Prompt」的專業提示詞工程師。目標受眾是台灣 6 歲小男孩，核心訴求是：視覺焦點 100% 鎖定在該國字本身、生動趣味、字形形變清晰、無多餘視覺雜訊。

# Goal
當使用者輸入任何一個繁體國字時，你只需分析該字意涵，並直接輸出一段精確、具體、可供 AI 影片生成模型（如 Sora, Runway Gen-3, Kling 等）使用的 **英文 Video Prompt**。

# Core Video Constraints (Strict)
1. 時長與主體：精確對應 8 秒短片。畫面中央 70% 必須始終是該國字本體，不可被多餘場景遮擋。
2. 視覺風格：3D Pixar / Disney 玩具風格，色彩鮮明（高對比、暖色或活潑色調），極簡乾淨的淺色純色背景（防止分散注意力）。
3. 形變邏輯（Morphing）：
   - 0~2秒：實物具象登場或文字登場（具備 6 歲男童喜愛的微彈性、果凍感、玩具感）。
   - 2~5秒：實物動態平滑轉化/變形成標準繁體國字，或國字長出對應特徵進行生動互動。
   - 5~8秒：國字定格在正中央，帶有輕微的呼吸動態（bouncing / pulsing），筆畫清晰、結構端正。
4. 嚴格禁止：不要複雜的背景故事、不要跑動的人物路人、不要閃爍刺眼的粒子雜訊。
5. 每個影片都要搭配該國字的發音，發音不需要注音，就把字持續的清楚念出來，可以配合動畫多唸幾次！

# Output Format
請一律使用以下固定格式輸出，不得加入多餘的寒暄或開場白：

【目標國字】：[使用者輸入的字]
【設計概念】：[一句話說明實物如何轉化為該字，以及為何吸引 6 歲男童]
【8s Video Prompt】:
```text
[8 秒精準英文提示詞，包含主體動作、形變過程、風格、鏡頭運動、背景要求]
---

# 開發規劃（重點方向）

## 1. 一句話定位

這個 repo 不是「一個 App」，是**一條產線 + 一個播放器**：
大人端把「字 → prompt → 影片檔 → 上架」跑順，小孩端只看到一個乾淨、好玩、不會亂跑的播放介面。

## 2. 兩個介面，一份資料

```
[Studio 大人端]  選字 → 生成 prompt → 我貼到訂閱 AI → 影片存回來 → 標記上架 → 看完給回饋
        │
        └── data/ (JSON，進 git) + media/ (影片圖片，不進 git)
        │
[Play 小孩端]    只有大按鈕、只有已上架的內容、每天有份量、看完會收集到「我的字」
```

關鍵：**資料是唯一真相**。每個字一筆記錄，走一條狀態機：

`待選 → prompt已生成 → 已投餵AI → 影片已入庫 → 已上架 → 有回饋`

## 3. 三個核心設計決策

**(a) Prompt 生成器 = 模板 + 字義資料，不是每次都問 AI**
把現有 prompt 拆成三層：不變的 `constraints`（8 秒、70% 佔比、Pixar 風、禁止項）、可調的 `style rules`（會被回饋修改的部分）、每個字獨有的 `concept`（實物 → 形變邏輯）。
生成 = 組裝，所以**可重現、可 diff、可回溯**。字義 concept 可以手寫，也可以呼叫 Claude API 產草稿再人工確認——但離線也要能跑。

**(b) 額度是第一公民（每天 3 支影片）**
不是「想到哪個字就生」，而是有一個**每日佇列**：依常用字頻 / 已學會的字 / 回饋不好要重做的字，自動排出今天該生的 3 個字。圖片額度多，就用圖片補「還沒排到影片」的字。

**(c) 回饋 → 規則庫，而不是回饋 → 存檔就沒了**
看完影片我按幾個標籤就好（「他超愛」「字看不清」「太吵/太亂」「形變沒看懂」「太快」）。
系統把標籤累積成**規則調整**（例：連 3 次「字看不清」→ 自動在 constraints 加強筆畫對比與定格秒數）。
下次生成自動帶入 + 把最受歡迎的 3 支當 few-shot 範例。這就是「相對自動精進」，不需要訓練模型。

## 4. 小孩端的鐵則（6-8 歲）

- **不打字、不選單**：首頁就是幾張大卡片，卡面就是那個字本身在動
- **不無限滑**：每天固定份量（例如 5-8 支），看完就是「今天結束了」動畫，不給無底洞
- **不知不覺學習**：不出考卷。用「收集」代替測驗——看過的字變成貼紙冊，隔幾天那個字會自己再冒出來（藏起來的間隔複習）
- **關不掉的邊界**：沒有外連、沒有留言、沒有推薦演算法、離線可用
- **聲音**：發音是重點，音量正常化，避免每支影片忽大忽小

## 5. 技術選型（建議直接定案）

| 項目 | 選擇 | 理由 |
|---|---|---|
| 前端 | Vite + React + TypeScript | 輕、build 出來是靜態檔，可直接丟平板/電視瀏覽器離線用 |
| 資料 | `data/*.json`（進 git） | 可 diff、可回溯、不用架 DB |
| 影片圖片 | `media/`（**加進 .gitignore**） | repo 就在 Google Drive 裡，檔案靠 Drive 同步，不要進 git |
| Studio | 同一個 web app 的 `/studio` 路由 | 不用維護兩套 |
| AI 呼叫 | 可選（有 key 就用 Claude API 生 concept 草稿） | 沒 key 也能用模板全手動跑 |

## 6. 里程碑

**M0 — 骨架（先能跑一輪）**
資料模型 + 字庫種子（先 50 個常用字）+ prompt 模板檔案化。輸出：一個 CLI，`輸入字 → 印出可直接貼的 prompt`。

**M1 — 影片入庫**
影片放進 `media/` 後，能登記到資料裡並標記上架。輸出：有 10 支影片的內容庫。

**M2 — 小孩端 MVP（最該優先做完的）**
大卡片首頁 + 播放器 + 每日份量 + 看完的收集動畫。**目標：兒子自己會用，我不用在旁邊。**

**M3 — 回饋迴路**
看完後的一鍵標籤 → 規則庫 → 下次 prompt 自動調整 + few-shot。

**M4 — 產線自動化 & 遊戲化**
每日 3 字自動排程、圖片模式、貼紙冊、間隔複習。

## 7. 已確認的前提（2026-09-04）

- **看的裝置：這台 MacBook**（不是平板、不是電視）
- **影片來源：我手動下載**（訂閱 AI 沒有 API）

這兩點讓架構可以大幅簡化：

**(a) 只要一個本機服務，`npm run dev` 就開工**
不用部署、不用 PWA、不用離線快取、不用管平板排版。一個本機 Node 服務同時掛 `/studio`（大人）和 `/play`（小孩），影片直接從 `media/` 串流。

**(b) 小孩端要防的不是網路，是「這台電腦」**
在瀏覽器上，鍵盤和其他分頁才是風險。所以 Play 模式要：
- 一進去就**全螢幕**，退出要長按（小孩按不住的那種）
- 鍵盤**只綁**方向鍵 / 空白鍵，其他全部吃掉
- 沒有網址列可打字、沒有任何外連

**(c) M1 的重點變成「匯入工具」**
因為是手動下載，最痛的是「一堆 `video_final_v3.mp4` 躺在下載資料夾」。所以做一個匯入流程：
掃 `~/Downloads` 找新影片 → 一次列出來給我看縮圖 → 我點「這是『山』」→ 自動改名、搬進 `media/`、寫回資料、標記上架。
**一次點完當天 3 支，30 秒收工。**

## 8. 修正後的起手順序

1. **M0 骨架**：資料模型 + prompt 模板檔案化 + `輸入字 → 印出 prompt` 的 CLI
2. **M2 小孩端 MVP**：全螢幕大卡片播放器（先用假資料也能做）
3. **M1 匯入工具**：把下載資料夾的影片一鍵歸位
4. M3 回饋迴路 → M4 自動化 / 貼紙冊 / 間隔複習

M3 等累積 20-30 支影片、真的看出問題模式再做，太早做會在猜。

---

# 進度（2026-09-04）

**M0 骨架 ✅** — 38 個象形字的字庫、prompt 三層模板、`npm run prompt` CLI
**M2 小孩端 ✅** — 入口 / 每日 6 張大卡 / 觀看 / 完成慶祝 / 貼紙冊 / 防呆
**M1 匯入 ✅** — 拖曳進 Studio 就自動改名入庫上架（`~/Downloads` 被 macOS 權限擋住，改用拖曳更穩）

實作時修掉的幾個真問題，記在這裡免得之後又犯：

- `requestFullscreen` 失敗會讓「開始」按了沒反應 → 先切畫面，全螢幕只當加分項
- 影片用 grid 排版會被 `place-items:center` 撐成正方形而**把字裁掉** → 改絕對定位 `inset: 0`
- 影片被擋住自動播 / 中途暫停 / 檔案壞掉，小孩會對著不動的畫面發呆 → 大播放鍵 + 自動退回字卡動畫
- 佇列原本以「出過 prompt」為準，一按就從今天的清單消失 → 改以「還沒有影片」為準
- 這台 Mac 沒有標楷體（BiauKai / DFKai-SB），實際是 `Kaiti TC` 在撐

## 第一輪改善（2026-09-05）

- 修正台灣日期比對；影片／字卡播完立即記錄，重播另外計次、貼紙去重，失敗儲存可重試。
- 匯入先留待確認；預覽原音或系統朗讀、設定每支音量、確認字形與聲音後上架。同類型可切換使用版本或停用，舊檔保留。
- 台灣日期的固定每日建議與最多三次製作紀錄；複製不扣額度，送出才記錄，失敗也保留已用額度。
- 送出保存 prompt／模板／概念快照，匯入可連結來源；圖片不視為影片完成。
- 四種負面回饋立即產生每字修訂，正面回饋不取消重做。舊版本不能清掉較新的負面回饋。
- 簡化單字結尾獎勵；完成文案改成「收集」，不把看過宣稱為學會。

操作以 README 為準。尚未實作音訊響度自動正規化。

## AI 創意生成（2026-09-05）

- Studio 與 CLI 改由本機已登入的 Codex、Claude Code 或 Google agy 生成，可指定模型、補充創意方向；原模板僅保留為明確選用的備案，AI 失敗不偷偷降級。
- 保留 8 秒、繁體正確字形與清楚發音等教學底線，場景、材質、笑點與節奏交由 AI 創作，不固定套三段式形變。
- 參考負面回饋、最近版本，以及最多三個有正面回饋且可追溯的 prompt 範例；尚未實作跨字規則自動學習。
- 新字只需輸入國字，AI 補上缺少的字義、注音與概念；既有人工字義／注音不被覆蓋。
- 每次結果保留來源、模型回報、創意方向與版本；製作紀錄保存選中版本，回饋改變後提示重新生成。
- 三種 CLI 均完成真實生成驗證；瀏覽器以隔離字庫驗證新增字、生成、版本保留、製作紀錄與回饋過期提示。

後續再評估圖片 prompt、真正逐筆畫形變與跨字規則學習。

## 小孩自主選字（2026-09-06）

- 首頁預設「我自己挑」，字庫每頁六字、可自由翻頁，看過的字仍可點選與重播。
- 「幫我挑」推薦最多六字，已上架影片優先於圖片與字卡，即使影片今天已看過也不排除。重新載入後依最新上架狀態修正舊推薦。
- 「下一個」不再跳過已看過的字；完成與貼紙冊都能回到選字頁，不以一天六字限制觀看。
- ⭐ 僅表示今天看過，卡片不變灰；每次完整重播累計觀看次數，貼紙仍去重。
